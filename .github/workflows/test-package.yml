# Install and test

name: Python package

on:
    push:
        branches:
            - develop
            - main
    pull_request:
        branches:
            - develop
            - main

jobs:
    test:
        runs-on: ubuntu-latest
        strategy:
            matrix:
                python_version:
                    - 3.7
                    - 3.8
                    - 3.9
                    - '3.10'
        steps:
            - uses: actions/checkout@v2
            - name: Set up Python ${{ matrix.python-version }}
              uses: actions/setup-python@v2
              with:
                python-version: ${{ matrix.python-version }}
            - name: DEBUG -- Check pip version
              run: python -m pip --version
            - name: DEBUG -- Check setuptools version
              run: python -m pip freeze | grep setuptools
            - name: Install dependencies
              run: python -m pip install -r requirements.txt .[dev]
            - name: Check docstrings
              run: python -m pydocstyle lazyscribe --convention=numpy
            - name: Check static typing
              run: python -m mypy lazyscribe
            - name: Run flake8
              run: python -m flake8 lazyscribe
            - name: Run unit testing
              run: python -m pytest tests --cov=./ --cov-report=xml
            - name: Run Codecov
              uses: codecov/codecov-action@v2
              with:
                token: ${{ secrets.CODECOV_TOKEN }}
                fail_ci_if_error: true
                files: ./coverage.xml
                verbose: true
